## Install Linux packages below, otherwhise pip install for pyscard will fail! 
### (Tested on Linux Mint 22.1 xia)

- Libs:
    - libpcsclite-dev
    - python3-dev

- Commands:
    ```python
        sudo apt-get install libpcsclite-dev && sudo apt-get install python3-dev
    ```